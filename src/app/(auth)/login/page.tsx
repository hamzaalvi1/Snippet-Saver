import { AuthCard } from "@/features/auth";

const LoginPage = () => {
  return (
    <AuthCard
      imgUrl="/assets/images/signup-image.jpg"
      element={<>HELLO WORLD</>}
    />
  );
};

export default LoginPage;
